<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.springwebapp.dao.mybatis.Ch13BoardDao">
	<!-- 실행할 때 parameterType객체를 받겠다는 설정 -->
	<insert id="insert" parameterType="com.mycompany.springwebapp.dto.Ch13Board">
		<!-- SQL문 제공 -->
		insert into board (bno, btitle, bcontent, mid, bdate, bhitcount)
		values(seq_bno.nextval, #{btitle}, #{bcontent}, #{mid}, sysdate, 0)
	</insert>
	
	<!-- 한 행을 resultType객체로 만든다. -->
	<select id="selectAll" resultType="com.mycompany.springwebapp.dto.Ch13Board">
		select bno, btitle, bcontent, mid, bdate, bhitcount, battachoname, battachsname, battachtype, battachdata
		from board
	</select>
	
	<update id="updateByBno" parameterType="com.mycompany.springwebapp.dto.Ch13Board">
		update board set btitle=#{btitle}, bcontent=#{bcontent}
		where bno=#{bno}
	</update>
	
	<delete id="deleteByBno" parameterType="int">
		delete from board where bno=#{bno} <!-- 한 값만 들어오기 때문에 #{dasd}도 상관없다 -->
	</delete>
</mapper>

